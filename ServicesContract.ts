import { IconType } from "react-icons";

export interface ServiceContract {
    title: string,
    about: string,
    Icon: IconType,
}

export interface SkillContract {
    name: string,
    level: string,
    Icon: IconType,
}